services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: brux
      POSTGRES_USER: brux
      POSTGRES_PASSWORD: brux
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
