{
  "name": "brux",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "db:generate": "turbo db:generate",
    "db:migrate": "turbo db:migrate",
    "ingest:news": "tsx scripts/ingestion/rss-news.ts",
    "ingest:events": "tsx scripts/ingestion/visit-brussels-events.ts"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
